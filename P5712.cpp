#include <bits/stdc++.h>
using namespace std;
int x;
int main() {
    cin >> x;
    if (x == 1 || x == 0) {
        cout << "Today, I ate " << x << " apple." << endl;
    } else {
        cout << "Today, I ate " << x << " apples." << endl;
    }
    return 0;
}
